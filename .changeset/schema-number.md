---
"@effect/tsgo": minor
---

Add a `schemaNumber` suggestion diagnostic for Effect v4 that recommends `Schema.Finite` and `Schema.FiniteFromString` over number schemas that also accept `NaN` and infinities.
